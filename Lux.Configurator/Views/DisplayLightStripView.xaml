<views:DisplayLightStripViewBase
  x:Class="Lux.Configurator.Views.DisplayLightStripView"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:views="using:Lux.Configurator.Views"
  xmlns:winUi="using:Microsoft.UI.Xaml.Controls"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  mc:Ignorable="d"
  d:DesignHeight="300"
  d:DesignWidth="400">
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="Auto"/>
      <ColumnDefinition Width="Auto"/>
      <ColumnDefinition Width="Auto"/>
    </Grid.ColumnDefinitions>
    <views:DisplayPositionView Grid.Column="0" ViewModel="{x:Bind ViewModel.Position}"/>
    <Grid Grid.Column="1" Margin="3,0">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>
      <TextBlock Text="LEDs:" Margin="3,0" VerticalAlignment="Center"/>
      <winUi:NumberBox Grid.Column="1" VerticalAlignment="Center" Value="{x:Bind ViewModel.LightCount, Mode=TwoWay}" ValidationMode="InvalidInputOverwritten" Minimum="0" Maximum="1000"
       ToolTipService.ToolTip="The number of lights on this segment."/>
    </Grid>
    <ToggleButton Grid.Column="2" Margin="6,0" IsChecked="{x:Bind ViewModel.IsEnabled, Mode=TwoWay}" Content="On"/>
  </Grid>
</views:DisplayLightStripViewBase>
